# 2026-03-08 — Heartbeat Notes

## TRK-71 (Node tagging, color coding, bookmarks) — DONE
- Engine methods: setNodeColor, setNodeColorBranch, bookmarkNode, unbookmarkNode, getBookmarks, createCustomTag, deleteCustomTag, listAllTags, bulk ops
- Components: ColorPicker, BookmarkBadge, BulkActionToolbar, TagCreator, TagBookmarkSidebar, BookmarkJumpOverlay
- SearchFilters extended with tags/colors/bookmarked
- Snapshot v2 schema with customTags
- 841 tests passing

## TRK-79 (Smart auto-layout algorithms) — DONE
- 7 layout algorithms: tree-vertical, tree-horizontal, compact, force-directed, mind-map, timeline, radial
- packages/svelte/src/lib/layout/types.ts, algorithms.ts, LayoutPicker.svelte
- TraekEngine.applyLayout() with rAF animation + SSR fallback
- 841 tests passing

## TRK-72 (Canvas snapshot & version history) — DONE
- packages/svelte/src/lib/versions/ — full version history system
- VersionHistoryManager: save/list/get/delete/export/import/diff
- AutoSnapshotTimer: configurable interval auto-snapshots
- StorageAdapter interface + 3 impls: Memory, LocalStorage, IndexedDB (traek-versions DB)
- snapshotDiff: pure structural diff (added/removed/changed nodes)
- 857 tests passing, lint clean

## Key patterns learned
- Each block lint rule: always add (key) to {#each} blocks in Svelte components
- VersionHistoryManager uses monotonic timestamp to avoid sort races in tests
- SSR guard for rAF: `typeof requestAnimationFrame === 'undefined'`
- `PAPERCLIP_API_KEY` + Python urllib.request for all API calls (no curl with env vars)
- 409 on checkout = skip forever
